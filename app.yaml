runtime: php73

handlers:
  - url: /assets
    static_dir: public/assets

  - url: /css
    static_dir: public/css

  - url: /fonts
    static_dir: public/fonts

  - url: /images
    static_dir: public/images

  - url: /img
    static_dir: public/img

  - url: /js
    static_dir: public/js

env_variables:
  APP_KEY: base64:RIAQizGOfACu71+WpxuIXe1l8Qy0opgD7dGe+tJxkdc=
  APP_STORAGE: /tmp
  VIEW_COMPILED_PATH: /tmp
  CACHE_DRIVER: database
  SESSION_DRIVER: database
  ## Set these environment variables according to your CloudSQL configuration.
  DB_DATABASE: dart
  DB_USERNAME: root
  DB_PASSWORD: 981014
  ## for MYSQL, use DB_SOCKET:
  DB_SOCKET: "/cloudsql/smooth-drive-281311:asia-east2:dart-db"
  ## for PostgreSQL, use DB_HOST:
  # DB_HOST: "/cloudsql/YOUR_CONNECTION_NAME"
  
